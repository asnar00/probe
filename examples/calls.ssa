fn square(x: i64) -> i64 {
entry:
    r: i64 = mul x, x
    ret r
}

; hypot2(a,b) = a*a + b*b, via two calls
fn hypot2(a: i64, b: i64) -> i64 {
entry:
    aa: i64 = call square(a)
    bb: i64 = call square(b)
    s: i64 = add aa, bb
    ret s
}
