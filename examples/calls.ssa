fn square(x: int) -> int {
^entry:
    r: int = imul x, x
    ret r
}

; hypot2(a,b) = a*a + b*b, via two calls
fn hypot2(a: int, b: int) -> int {
^entry:
    aa: int = square(a)
    bb: int = square(b)
    s: int = iadd aa, bb
    ret s
}
