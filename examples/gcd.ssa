; Euclid's algorithm — exercises rem and a swap-shaped loop-carried pair
fn gcd(a: i64, b: i64) -> i64 {
entry:
    jmp loop(a, b)
loop(x: i64, y: i64):
    done: u1 = cmp.eq y, 0
    br done, exit, step
step:
    r: i64 = rem x, y
    jmp loop(y, r)
exit:
    ret x
}
