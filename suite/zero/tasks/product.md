# product
*what the product sets and no feature says: the tasks store's bounds*

A `bound <function words>: N` line gives every loop of that function a trip count the IR does not show on its own, so `probe cost` counts it; the code never states the number (zero.md section 1, log 41). The two counting tasks are wired at 3, 4 and 5 in `tasks.zero`.

bound count down from: 5
bound count up to: 5
