# riscv64 encoding scorecard

Generated by `probe scorecard riscv64` from `targets/riscv64.encodings.json` against
the official table (`tools/get-isa-tables.sh`). The learner never reads the
table; this is the check afterwards.

## Learned templates: 94

| template | official encoding | fields |
|---|---|---|
| `add {r}, {r}, {r}` | add | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `sub {r}, {r}, {r}` | sub, neg (sub) | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `and {r}, {r}, {r}` | and | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `or {r}, {r}, {r}` | or | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `xor {r}, {r}, {r}` | xor | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `sll {r}, {r}, {r}` | sll | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `srl {r}, {r}, {r}` | srl | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `sra {r}, {r}, {r}` | sra | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `mul {r}, {r}, {r}` | mul | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `div {r}, {r}, {r}` | div | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `divu {r}, {r}, {r}` | divu | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `rem {r}, {r}, {r}` | rem | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `remu {r}, {r}, {r}` | remu | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `addi {r}, {r}, {i -2048..2047}` | addi, mv (addi), nop (addi) | `{r}`→rd, `{r}`→rs1, `{i -2048..2047}`→imm12 |
| `andi {r}, {r}, {i -2048..2047}` | andi | `{r}`→rd, `{r}`→rs1, `{i -2048..2047}`→imm12 |
| `ori {r}, {r}, {i -2048..2047}` | ori | `{r}`→rd, `{r}`→rs1, `{i -2048..2047}`→imm12 |
| `xori {r}, {r}, {i -2048..2047}` | xori | `{r}`→rd, `{r}`→rs1, `{i -2048..2047}`→imm12 |
| `slti {r}, {r}, {i -2048..2047}` | slti | `{r}`→rd, `{r}`→rs1, `{i -2048..2047}`→imm12 |
| `slli {r}, {r}, {i 0..63}` | slli | `{r}`→rd, `{r}`→rs1, `{i 0..63}`→shamtd |
| `srli {r}, {r}, {i 0..63}` | srli | `{r}`→rd, `{r}`→rs1, `{i 0..63}`→shamtd |
| `srai {r}, {r}, {i 0..63}` | srai | `{r}`→rd, `{r}`→rs1, `{i 0..63}`→shamtd |
| `lui {r}, {i 0..1048575}` | lui | `{r}`→rd, `{i 0..1048575}`→imm20 |
| `ld {r}, {i -2048..2047}({r})` | ld | `{r}`→rd, `{i -2048..2047}`→imm12, `{r}`→rs1 |
| `sd {r}, {i -2048..2047}({r})` | sd | `{r}`→rs2, `{i -2048..2047}`→imm12hi+imm12lo, `{r}`→rs1 |
| `lw {r}, {i -2048..2047}({r})` | lw | `{r}`→rd, `{i -2048..2047}`→imm12, `{r}`→rs1 |
| `lwu {r}, {i -2048..2047}({r})` | lwu | `{r}`→rd, `{i -2048..2047}`→imm12, `{r}`→rs1 |
| `sw {r}, {i -2048..2047}({r})` | sw | `{r}`→rs2, `{i -2048..2047}`→imm12hi+imm12lo, `{r}`→rs1 |
| `lb {r}, {i -2048..2047}({r})` | lb | `{r}`→rd, `{i -2048..2047}`→imm12, `{r}`→rs1 |
| `lbu {r}, {i -2048..2047}({r})` | lbu | `{r}`→rd, `{i -2048..2047}`→imm12, `{r}`→rs1 |
| `lh {r}, {i -2048..2047}({r})` | lh | `{r}`→rd, `{i -2048..2047}`→imm12, `{r}`→rs1 |
| `lhu {r}, {i -2048..2047}({r})` | lhu | `{r}`→rd, `{i -2048..2047}`→imm12, `{r}`→rs1 |
| `sb {r}, {i -2048..2047}({r})` | sb | `{r}`→rs2, `{i -2048..2047}`→imm12hi+imm12lo, `{r}`→rs1 |
| `sh {r}, {i -2048..2047}({r})` | sh | `{r}`→rs2, `{i -2048..2047}`→imm12hi+imm12lo, `{r}`→rs1 |
| `beq {r}, {r}, {i -4096..4094 /2}` | beq, beqz (beq) | `{r}`→rs1, `{r}`→rs2, `{i -4096..4094 /2}`→bimm12hi+bimm12lo |
| `bne {r}, {r}, {i -4096..4094 /2}` | bne, bnez (bne) | `{r}`→rs1, `{r}`→rs2, `{i -4096..4094 /2}`→bimm12hi+bimm12lo |
| `blt {r}, {r}, {i -4096..4094 /2}` | blt, bgt (blt), bgtz (blt), bltz (blt) | `{r}`→rs1, `{r}`→rs2, `{i -4096..4094 /2}`→bimm12hi+bimm12lo |
| `bge {r}, {r}, {i -4096..4094 /2}` | bge, ble (bge), bgez (bge), blez (bge) | `{r}`→rs1, `{r}`→rs2, `{i -4096..4094 /2}`→bimm12hi+bimm12lo |
| `jal {r}, {i -1048576..1048574 /2}` | jal, j (jal) | `{r}`→rd, `{i -1048576..1048574 /2}`→jimm20 |
| `jalr {r}, {i -2048..2047}({r})` | jalr, jr (jalr) | `{r}`→rd, `{i -2048..2047}`→imm12, `{r}`→rs1 |
| `slt {r}, {r}, {r}` | slt, sltz (slt), sgtz (slt) | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `sltu {r}, {r}, {r}` | sltu, snez (sltu) | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `sltiu {r}, {r}, {i -2048..2047}` | sltiu | `{r}`→rd, `{r}`→rs1, `{i -2048..2047}`→imm12 |
| `addw {r}, {r}, {r}` | addw | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `subw {r}, {r}, {r}` | subw | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `mulw {r}, {r}, {r}` | mulw | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `divw {r}, {r}, {r}` | divw | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `divuw {r}, {r}, {r}` | divuw | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `remw {r}, {r}, {r}` | remw | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `remuw {r}, {r}, {r}` | remuw | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `sllw {r}, {r}, {r}` | sllw | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `srlw {r}, {r}, {r}` | srlw | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `sraw {r}, {r}, {r}` | sraw | `{r}`→rd, `{r}`→rs1, `{r}`→rs2 |
| `addiw {r}, {r}, {i -2048..2047}` | addiw, sext.w (addiw) | `{r}`→rd, `{r}`→rs1, `{i -2048..2047}`→imm12 |
| `fadd.s {f}, {f}, {f}` | fadd.s | `{f}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `fsub.s {f}, {f}, {f}` | fsub.s | `{f}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `fmul.s {f}, {f}, {f}` | fmul.s | `{f}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `fdiv.s {f}, {f}, {f}` | fdiv.s | `{f}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `fadd.d {f}, {f}, {f}` | fadd.d | `{f}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `fsub.d {f}, {f}, {f}` | fsub.d | `{f}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `fmul.d {f}, {f}, {f}` | fmul.d | `{f}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `fdiv.d {f}, {f}, {f}` | fdiv.d | `{f}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `fsqrt.s {f}, {f}` | fsqrt.s | `{f}`→rd, `{f}`→rs1 |
| `fsqrt.d {f}, {f}` | fsqrt.d | `{f}`→rd, `{f}`→rs1 |
| `fneg.s {f}, {f}` | fsgnjn.s, fneg.s (fsgnjn.s) | `{f}`→rd, `{f}`→rs1+rs2 |
| `fneg.d {f}, {f}` | fsgnjn.d, fneg.d (fsgnjn.d) | `{f}`→rd, `{f}`→rs1+rs2 |
| `fabs.s {f}, {f}` | fsgnjx.s, fabs.s (fsgnjx.s) | `{f}`→rd, `{f}`→rs1+rs2 |
| `fabs.d {f}, {f}` | fsgnjx.d, fabs.d (fsgnjx.d) | `{f}`→rd, `{f}`→rs1+rs2 |
| `fmadd.s {f}, {f}, {f}, {f}` | fmadd.s | `{f}`→rd, `{f}`→rs1, `{f}`→rs2, `{f}`→rs3 |
| `fmadd.d {f}, {f}, {f}, {f}` | fmadd.d | `{f}`→rd, `{f}`→rs1, `{f}`→rs2, `{f}`→rs3 |
| `feq.s {r}, {f}, {f}` | feq.s | `{r}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `flt.s {r}, {f}, {f}` | flt.s | `{r}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `fle.s {r}, {f}, {f}` | fle.s | `{r}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `feq.d {r}, {f}, {f}` | feq.d | `{r}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `flt.d {r}, {f}, {f}` | flt.d | `{r}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `fle.d {r}, {f}, {f}` | fle.d | `{r}`→rd, `{f}`→rs1, `{f}`→rs2 |
| `fcvt.d.s {f}, {f}` | fcvt.d.s | `{f}`→rd, `{f}`→rs1 |
| `fcvt.s.d {f}, {f}` | fcvt.s.d | `{f}`→rd, `{f}`→rs1 |
| `fcvt.s.w {f}, {r}` | fcvt.s.w | `{f}`→rd, `{r}`→rs1 |
| `fcvt.s.wu {f}, {r}` | fcvt.s.wu | `{f}`→rd, `{r}`→rs1 |
| `fcvt.s.l {f}, {r}` | fcvt.s.l | `{f}`→rd, `{r}`→rs1 |
| `fcvt.s.lu {f}, {r}` | fcvt.s.lu | `{f}`→rd, `{r}`→rs1 |
| `fcvt.d.w {f}, {r}` | fcvt.d.w | `{f}`→rd, `{r}`→rs1 |
| `fcvt.d.wu {f}, {r}` | fcvt.d.wu | `{f}`→rd, `{r}`→rs1 |
| `fcvt.d.l {f}, {r}` | fcvt.d.l | `{f}`→rd, `{r}`→rs1 |
| `fcvt.d.lu {f}, {r}` | fcvt.d.lu | `{f}`→rd, `{r}`→rs1 |
| `fmv.w.x {f}, {r}` | fmv.w.x, fmv.s.x (fmv.w.x) | `{f}`→rd, `{r}`→rs1 |
| `fmv.x.w {r}, {f}` | fmv.x.w, fmv.x.s (fmv.x.w) | `{r}`→rd, `{f}`→rs1 |
| `fmv.d.x {f}, {r}` | fmv.d.x | `{f}`→rd, `{r}`→rs1 |
| `fmv.d {f}, {f}` | fsgnj.d, fmv.d (fsgnj.d) | `{f}`→rd, `{f}`→rs1+rs2 |
| `fsd {f}, {i -2048..2047}({r})` | fsd | `{f}`→rs2, `{i -2048..2047}`→imm12hi+imm12lo, `{r}`→rs1 |
| `fld {f}, {i -2048..2047}({r})` | fld | `{f}`→rd, `{i -2048..2047}`→imm12, `{r}`→rs1 |
| `fmv.x.d {r}, {f}` | fmv.x.d | `{r}`→rd, `{f}`→rs1 |
| `csrrs {r}, {i 0..4095}, {r}` | csrrs, csrr (csrrs), csrs (csrrs) | `{r}`→rd, `{i 0..4095}`→csr, `{r}`→rs1 |
| `auipc {r}, {i 0..1048575}` | auipc | `{r}`→rd, `{i 0..1048575}`→imm20 |

94 of 94 templates match an official encoding with their fields inside its operand fields; 0 problem(s).

## Coverage of the official inventory

An instruction usually has several encodings (widths, operand forms); a
mnemonic counts as learned when any of its encodings is.

| group | encodings | learned | mnemonics | learned |
|---|---|---|---|---|
| rv64_a | 11 | 0 | 11 | 0 |
| rv64_d | 6 | 4 | 6 | 4 |
| rv64_f | 4 | 2 | 4 | 2 |
| rv64_i | 16 | 13 | 16 | 13 |
| rv64_m | 5 | 5 | 5 | 5 |
| rv_a | 11 | 0 | 11 | 0 |
| rv_d | 29 | 21 | 29 | 21 |
| rv_f | 39 | 19 | 39 | 19 |
| rv_i | 64 | 48 | 62 | 48 |
| rv_m | 8 | 5 | 8 | 5 |
| rv_zicsr | 13 | 3 | 13 | 3 |
| rv_zifencei | 1 | 0 | 1 | 0 |

Not learned in rv64_a (11 mnemonics): amoadd.d, amoand.d, amomax.d, amomaxu.d, amomin.d, amominu.d, amoor.d, amoswap.d, amoxor.d, lr.d, sc.d

Not learned in rv64_d (2 mnemonics): fcvt.l.d, fcvt.lu.d

Not learned in rv64_f (2 mnemonics): fcvt.l.s, fcvt.lu.s

Not learned in rv64_i (3 mnemonics): slliw, sraiw, srliw

Not learned in rv_a (11 mnemonics): amoadd.w, amoand.w, amomax.w, amomaxu.w, amomin.w, amominu.w, amoor.w, amoswap.w, amoxor.w, lr.w, sc.w

Not learned in rv_d (8 mnemonics): fclass.d, fcvt.w.d, fcvt.wu.d, fmax.d, fmin.d, fmsub.d, fnmadd.d, fnmsub.d

Not learned in rv_f (20 mnemonics): fclass.s, fcvt.w.s, fcvt.wu.s, flw, fmax.s, fmin.s, fmsub.s, fmv.s, fnmadd.s, fnmsub.s, frcsr, frflags, frrm, fscsr, fsflags, fsflagsi, fsgnj.s, fsrm, fsrmi, fsw

Not learned in rv_i (14 mnemonics): bgeu, bgtu, bleu, bltu, ebreak, ecall, fence, fence.tso, pause, ret, sbreak, scall, seqz, zext.b

Not learned in rv_m (3 mnemonics): mulh, mulhsu, mulhu

Not learned in rv_zicsr (10 mnemonics): csrc, csrci, csrrc, csrrci, csrrsi, csrrw, csrrwi, csrsi, csrw, csrwi

Not learned in rv_zifencei (1 mnemonics): fence.i

